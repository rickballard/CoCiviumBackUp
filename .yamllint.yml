---
extends: default
rules:
  document-start: disable
  truthy: disable
  new-lines:
    type: unix
  line-length: disable
  empty-lines:
    max: 1
    level: error
  braces: disable
  colons: disable

